# Core
fastapi[all]>=0.111.0
uvicorn[standard]>=0.29.0
pydantic>=2.0.0
pydantic-settings>=2.0.0
python-dotenv>=1.0.0
loguru>=0.7.0

# LLM Providers
anthropic>=0.28.0
openai>=1.30.0
google-generativeai>=0.7.0
httpx>=0.27.0

# Database (optional — needed for persistent runs)
sqlalchemy>=2.0.0
aiosqlite>=0.20.0

# Task queue (optional — needed for worker mode)
celery[redis]>=5.3.0
redis>=5.0.0

# Dev / test
pytest>=8.0.0
pytest-asyncio>=0.23.0
